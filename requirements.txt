# Q2: document schema, loading, cleaning, chunking, offline embeddings,
# local JSON vector store, and retrieval.
#
# Embeddings and the vector index are implemented with the standard library
# (hashed n-grams + JSON). Do not add Chroma/FAISS/API clients unless a
# later stage actually uses them.

pyyaml>=6.0,<7.0      # parse YAML front matter in synthetic Markdown docs
pytest>=7.4,<9.0       # test runner
python-dotenv>=1.0,<2.0 # load local LiveKit configuration without hardcoding secrets
livekit-agents>=1.4,<2.0 # Q1 voice runtime, function tools, and Cloud inference
